import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  title: {
    fontSize: 18,
    color: 'black',
  },
  divider: {
    borderWidth: 1,
    borderColor: 'gray',
    width: 360,
    marginTop: 8
  },
});